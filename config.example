# - makefile -
# (C) 2023 Joerg Jungermann, GPLv2 see LICENSE

# EXAMPLE config
# copy to ./config

# ignore auto-build of docker images, as they will be automatically build
# as base of this config
DOCKER_IMAGES :=
# reset WORK_FILES and add which images we want
WORK_FILES :=

#DOCKER_IMAGES += rk1-armbian-zfs
#DOCKER_IMAGES += armhf-OptAlpine-3.22

# generate specific images in docker
#DOCKER_IMAGES += $(shell lib/tgt.sh PLATFORM-* )

# X6100 specifics - minfree space of rootfs in KBytes
X6100_MIN_FREE=512

# generate specific image files
#WORK_FILES += $(shell lib/tgt.sh amd64-*                img.zst )
#WORK_FILES += $(shell lib/tgt.sh amd64-EFI-CI-trixie*   img.zst  EFI.iso )

#WORK_FILES += $(shell lib/tgt.sh bpi2m0-alpine*     sqfs.img.zst sqfs ) # img.zst rootfs.tar.zst
#WORK_FILES += $(shell lib/tgt.sh turingpi2-alpine*  sqfs.img.zst sqfs )
#WORK_FILES += $(shell lib/tgt.sh rk1-alpine*        sqfs.img.zst sqfs )

#WORK_FILES += $(shell lib/tgt.sh rpi-alpine*        sqfs.img.zst sqfs )


# Addtional targets for external resources

#input/rk1-trixie-edge.img:
#	cp ../armbian-build/output/images/Armbian-unofficial_25.11.0-trunk_Turing-rk1_trixie_edge_6.17.4-ci_minimal.img $@

#input/bpi2m0-Armbian-25.02-bookworm-minimal.img.xz:
#	cp ../Blobs/bpi2m0-Armbian-25.02-bookworm-minimal.img.xz $@

#input/amd64-Debian-Trixie-Cloud-Image.img:
#	cp ../sbc-blobs/amd64-Debian-Trixie-Cloud-Image.img $@

# vim: noet ts=2 sw=2 ft=Makefile
